<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <ul>
      <% todos.forEach((todo)=>{%>
      <li
        ><a href="/todos/<%= todo._id %>"
          ><%= todo.title %>:<%= todo.description %>.</a
        >
        <button onclick="window.location = '/todos/<%= todo._id %>/edit';"
          >edit</button
        >
        <form
          action="/todos/<%= todo._id %>/delete?_method=delete"
          method="post"
          ><button type="submit">delete</button></form
        >
      </li>
      <% }) %>
    </ul>
    <a href="/todos/new">create new todo</a>
  </body>
</html>
